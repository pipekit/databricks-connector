## databricks-connector submit

Submit a Databricks job run

```
databricks-connector submit [flags]
```

### Options

```
      --args strings                       List of positional arguments for the job (overrides parameters)
      --availability string                Availability (SPOT, ON_DEMAND)
      --cloud-provider string              Cloud provider (AWS, AZURE, GCP)
      --cluster-mode string                Cluster mode (Existing, New, Serverless) (default "New")
      --code-path string                   Path to the code (Notebook path or Python file path)
      --dry-run                            Print the job submission request as JSON and exit without submitting
      --email-notifications strings        List of emails for notifications
      --existing-cluster-id string         ID of an existing cluster to reuse
  -h, --help                               help for submit
      --instance-pool-id string            Instance pool ID for new cluster
      --main-class-name string             Main class name for spark-jar task
      --max-workers int                    Max workers for autoscale (default 2)
      --min-workers int                    Min workers for autoscale (default 1)
      --new-cluster-node-type string       Node type for new cluster (e.g., i3.xlarge)
      --new-cluster-num-workers int        Number of workers for new cluster (default 1)
      --new-cluster-spark-version string   Spark version for new cluster
      --parameters strings                 Parameters for the job (key=value)
      --policy-id string                   Policy ID for new cluster
      --run-name string                    Name of the job run (default "argo-workflow-run")
      --scaling-type string                Scaling type (fixed, autoscale) (default "fixed")
      --spark-conf strings                 Spark configuration (key=value)
      --task-key string                    Task key for the job task (default "main_task")
      --task-type string                   Type of task: notebook or spark-python (default "notebook")
```

### SEE ALSO

* [databricks-connector](databricks-connector.md)	 - A Databricks connector for Argo Workflows

###### Auto generated by spf13/cobra on 22-Jan-2026
